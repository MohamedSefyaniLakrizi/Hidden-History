import SwiftUI

/// Placeholder root view — replaced in Step 14 with the full navigation shell.
struct RootView: View {
    var body: some View {
        NavigationStack {
            Text("Hidden History")
                .navigationTitle("Hidden History")
        }
    }
}
